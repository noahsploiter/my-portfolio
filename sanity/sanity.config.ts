import {deskTool} from 'sanity/desk'
import {schemaTypes} from './schemas'


export default ({
  name: 'default',
  title: 'Sanity project',

  projectId: 'ipueba9v',
  dataset: 'production',


  schema: {
    types: schemaTypes,
  },
})
